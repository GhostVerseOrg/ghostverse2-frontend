query PrivacyPolicyContent($locale: I18NLocaleCode) {
  privacyPolicy(locale: $locale) {
    data {
      id
      attributes {
        createdAt
        publishedAt
        updatedAt
        locale
        LegalContent
      }
    }
  }
}
