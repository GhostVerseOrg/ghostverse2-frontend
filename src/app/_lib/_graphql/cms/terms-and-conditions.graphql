query TermsOfUseContent($locale: I18NLocaleCode) {
  termsOfUse(locale: $locale) {
    data {
      id
      attributes {
        createdAt
        publishedAt
        updatedAt
        locale
        LegalContent
      }
    }
  }
}
